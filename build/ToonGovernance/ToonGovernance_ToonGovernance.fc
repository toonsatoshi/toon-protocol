#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; ToonGovernance_ToonGovernance.headers.fc
;;
;; Header files for ToonGovernance
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_store_address_opt
builder __tact_store_address_opt(builder b, slice address) inline;

;; __tact_not_null
forall X -> X __tact_not_null(X x) impure inline;

;; __tact_crc16
(slice) __tact_crc16(slice data) inline_ref;

;; __tact_base64_encode
(slice) __tact_base64_encode(slice data) inline_ref;

;; __tact_address_to_user_friendly
(slice) __tact_address_to_user_friendly(slice address) inline_ref;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_slice_eq
int __tact_slice_eq(slice a, slice b) inline;

;; __tact_string_builder_start
tuple __tact_string_builder_start(builder b) inline;

;; __tact_string_builder_start_string
tuple __tact_string_builder_start_string() inline;

;; __tact_string_builder_end
cell __tact_string_builder_end(tuple builders) inline;

;; __tact_string_builder_end_slice
slice __tact_string_builder_end_slice(tuple builders) inline;

;; __tact_string_builder_append
((tuple), ()) __tact_string_builder_append(tuple builders, slice sc) inline_ref;

;; __tact_dict_get_slice_int
int __tact_dict_get_slice_int(cell d, int kl, slice k, int vl) inline;

;; __tact_dict_set_slice_int
(cell, ()) __tact_dict_set_slice_int(cell d, int kl, slice k, int v, int vl) inline;

;; __tact_dict_get_int_int
int __tact_dict_get_int_int(cell d, int kl, int k, int vl) inline;

;; __tact_dict_set_int_int
(cell, ()) __tact_dict_set_int_int(cell d, int kl, int k, int v, int vl) inline;

;; __tact_dict_get_int_cell
cell __tact_dict_get_int_cell(cell d, int kl, int k) inline;

;; __tact_dict_set_int_cell
(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline;

;; $DeployOk$_store
builder $DeployOk$_store(builder build_0, (int) v) inline;

;; $DeployOk$_store_cell
cell $DeployOk$_store_cell((int) v, builder b) inline;

;; $TokenTransfer$_store
builder $TokenTransfer$_store(builder build_0, (int, int, slice, slice, cell, int, slice) v) inline;

;; $TokenTransfer$_store_cell
cell $TokenTransfer$_store_cell((int, int, slice, slice, cell, int, slice) v, builder b) inline;

;; $UpdateMintAuthority$_store
builder $UpdateMintAuthority$_store(builder build_0, (slice) v) inline;

;; $UpdateMintAuthority$_store_cell
cell $UpdateMintAuthority$_store_cell((slice) v, builder b) inline;

;; $UpdateConfigParam$_store
builder $UpdateConfigParam$_store(builder build_0, (slice, int) v) inline;

;; $UpdateConfigParam$_store_cell
cell $UpdateConfigParam$_store_cell((slice, int) v, builder b) inline;

;; $GlobalProposal$_store
builder $GlobalProposal$_store(builder build_0, (slice, int, slice, slice, int, int, int, int) v) inline;

;; $GlobalProposal$_store_cell
cell $GlobalProposal$_store_cell((slice, int, slice, slice, int, int, int, int) v, builder b) inline;

;; $GlobalProposal$_load
(slice, ((slice, int, slice, slice, int, int, int, int))) $GlobalProposal$_load(slice sc_0) inline;

;; $GlobalProposal$_as_optional
tuple $GlobalProposal$_as_optional((slice, int, slice, slice, int, int, int, int) v) inline;

;; $GlobalProposal$_load_opt
tuple $GlobalProposal$_load_opt(cell cl) inline;

;; $AddressProposal$_store
builder $AddressProposal$_store(builder build_0, (slice, slice, slice, slice, int, int, int, int) v) inline;

;; $AddressProposal$_store_cell
cell $AddressProposal$_store_cell((slice, slice, slice, slice, int, int, int, int) v, builder b) inline;

;; $AddressProposal$_load
(slice, ((slice, slice, slice, slice, int, int, int, int))) $AddressProposal$_load(slice sc_0) inline;

;; $AddressProposal$_as_optional
tuple $AddressProposal$_as_optional((slice, slice, slice, slice, int, int, int, int) v) inline;

;; $AddressProposal$_load_opt
tuple $AddressProposal$_load_opt(cell cl) inline;

;; $ToonGovernance$_store
builder $ToonGovernance$_store(builder build_0, (slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) v) inline;

;; $ToonGovernance$_load
(slice, ((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell))) $ToonGovernance$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $GlobalProposal$_not_null
((slice, int, slice, slice, int, int, int, int)) $GlobalProposal$_not_null(tuple v) inline;

;; $GlobalProposal$_to_tuple
tuple $GlobalProposal$_to_tuple(((slice, int, slice, slice, int, int, int, int)) v) inline;

;; $GlobalProposal$_to_opt_tuple
tuple $GlobalProposal$_to_opt_tuple(tuple v) inline;

;; $GlobalProposal$_to_opt_external
tuple $GlobalProposal$_to_opt_external(tuple v) inline;

;; $AddressProposal$_not_null
((slice, slice, slice, slice, int, int, int, int)) $AddressProposal$_not_null(tuple v) inline;

;; $AddressProposal$_to_tuple
tuple $AddressProposal$_to_tuple(((slice, slice, slice, slice, int, int, int, int)) v) inline;

;; $AddressProposal$_to_opt_tuple
tuple $AddressProposal$_to_opt_tuple(tuple v) inline;

;; $AddressProposal$_to_opt_external
tuple $AddressProposal$_to_opt_external(tuple v) inline;

;; $ToonJettonWallet$init$_store
builder $ToonJettonWallet$init$_store(builder build_0, (int, slice, slice) v) inline;

;; $ToonGovernance$init$_load
(slice, ((slice, slice, slice))) $ToonGovernance$init$_load(slice sc_0) inline;

;; $ToonGovernance$_contract_init
(slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $ToonGovernance$_contract_init(slice $registry, slice $vault, slice $jettonMaster) impure inline;

;; $ToonGovernance$_contract_load
(slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $ToonGovernance$_contract_load() impure inline;

;; $ToonGovernance$_contract_store
() $ToonGovernance$_contract_store((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) v) impure inline;

;; $Cell$_fun_asSlice
slice $Cell$_fun_asSlice(cell $self) impure inline;

;; $global_newAddress
slice $global_newAddress(int $chain, int $hash) impure inline;

;; $global_contractAddressExt
slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline;

;; $global_contractAddress
slice $global_contractAddress((cell, cell) $s) impure inline;

;; $MessageParameters$_constructor_bounce_to_value_mode_body
((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline;

;; $ToonJettonWallet$_init_child
(cell, cell) $ToonJettonWallet$_init_child(int $balance, slice $owner, slice $master) inline_ref;

;; $ToonGovernance$_fun_voteKey
((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_voteKey((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId, slice $voter) impure inline_ref;

;; $ToonGovernance$_fun_quorumPassed
((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_quorumPassed((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, (slice, int, slice, slice, int, int, int, int) $proposal) impure inline_ref;

;; $ToonGovernance$_fun_quorumPassedAddr
((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_quorumPassedAddr((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, (slice, slice, slice, slice, int, int, int, int) $proposal) impure inline_ref;

;; $ToonGovernance$_fun_totalStaked
((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_totalStaked((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self) impure inline_ref;

;; $ToonGovernance$_fun_stake
((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_stake((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, slice $voter) impure inline_ref;

;; $ToonGovernance$_fun_getProposal
((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), tuple) $ToonGovernance$_fun_getProposal((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId) impure inline_ref;

;; $ToonGovernance$_fun_getAddressProposal
((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), tuple) $ToonGovernance$_fun_getAddressProposal((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId) impure inline_ref;

;; $ToonGovernance$_fun_hasAddressVoted
((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_hasAddressVoted((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId, slice $voter) impure inline_ref;

;; $ToonGovernance$_fun_hasAddressVotedOnAddressProposal
((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_hasAddressVotedOnAddressProposal((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId, slice $voter) impure inline_ref;

;; $ToonGovernance$_fun_quorumMet
((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_quorumMet((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId) impure inline_ref;

;; $ToonGovernance$_fun_notify
((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), ()) $ToonGovernance$_fun_notify((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, cell $body) impure inline;

;; $SendParameters$_constructor_to_value_mode_body
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline;

;; $TokenTransfer$_constructor_queryId_amount_destination_response_destination_customPayload_forward_ton_amount_forward_payload
((int, int, slice, slice, cell, int, slice)) $TokenTransfer$_constructor_queryId_amount_destination_response_destination_customPayload_forward_ton_amount_forward_payload(int $queryId, int $amount, slice $destination, slice $response_destination, cell $customPayload, int $forward_ton_amount, slice $forward_payload) inline;

;; $GlobalProposal$_constructor_parameter_newValue_description_proposer_votesFor_votesAgainst_deadline_executed
((slice, int, slice, slice, int, int, int, int)) $GlobalProposal$_constructor_parameter_newValue_description_proposer_votesFor_votesAgainst_deadline_executed(slice $parameter, int $newValue, slice $description, slice $proposer, int $votesFor, int $votesAgainst, int $deadline, int $executed) inline;

;; $UpdateConfigParam$_constructor_parameter_newValue
((slice, int)) $UpdateConfigParam$_constructor_parameter_newValue(slice $parameter, int $newValue) inline;

;; $AddressProposal$_constructor_parameter_newAddress_description_proposer_votesFor_votesAgainst_deadline_executed
((slice, slice, slice, slice, int, int, int, int)) $AddressProposal$_constructor_parameter_newAddress_description_proposer_votesFor_votesAgainst_deadline_executed(slice $parameter, slice $newAddress, slice $description, slice $proposer, int $votesFor, int $votesAgainst, int $deadline, int $executed) inline;

;; $UpdateMintAuthority$_constructor_newAuthority
((slice)) $UpdateMintAuthority$_constructor_newAuthority(slice $newAuthority) inline;

;; $DeployOk$_constructor_queryId
((int)) $DeployOk$_constructor_queryId(int $queryId) inline;


;; ToonGovernance_ToonGovernance.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

int __tact_sha256(slice data) asm """

            <{
                <{ DUP SREFS }> PUSHCONT
                <{ LDREFRTOS }> PUSHCONT
                WHILE
                DEPTH
                HASHEXT_SHA256
            }> PUSHCONT
            1 1 CALLXARGS

""";

builder __tact_store_addr_none(builder b) asm "b{00} STSLICECONST";

builder __tact_store_address_opt(builder b, slice address) inline {
    if (null?(address)) {
        return __tact_store_addr_none(b);
    } else {
        return b.store_slice(address);
    }
}

forall X -> X __tact_not_null(X x) impure inline {
    throw_if(128, null?(x)); return x;
}

(cell, int) __tact_dict_delete(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTDEL
""";

(slice, int) __tact_dict_get(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTGET NULLSWAPIFNOT
""";

(slice) __tact_preload_offset(slice s, int offset, int bits) asm """
    SDSUBSTR
""";

(slice) __tact_crc16(slice data) inline_ref {
    slice new_data = begin_cell()
        .store_slice(data)
        .store_slice("0000"s)
    .end_cell().begin_parse();
    int reg = 0;
    while (~ new_data.slice_data_empty?()) {
        int byte = new_data~load_uint(8);
        int mask = 0x80;
        while (mask > 0) {
            reg <<= 1;
            if (byte & mask) {
                reg += 1;
            }
            mask >>= 1;
            if (reg > 0xffff) {
                reg &= 0xffff;
                reg ^= 0x1021;
            }
        }
    }
    (int q, int r) = divmod(reg, 256);
    return begin_cell()
        .store_uint(q, 8)
        .store_uint(r, 8)
    .end_cell().begin_parse();
}

(slice) __tact_base64_encode(slice data) inline_ref {
    slice chars = "4142434445464748494A4B4C4D4E4F505152535455565758595A6162636465666768696A6B6C6D6E6F707172737475767778797A303132333435363738392D5F"s;
    builder res = begin_cell();

    while (data.slice_bits() >= 24) {
        (int bs1, int bs2, int bs3) = (data~load_uint(8), data~load_uint(8), data~load_uint(8));

        int n = (bs1 << 16) | (bs2 << 8) | bs3;

        res = res
            .store_slice(__tact_preload_offset(chars, ((n >> 18) & 63) * 8, 8))
            .store_slice(__tact_preload_offset(chars, ((n >> 12) & 63) * 8, 8))
            .store_slice(__tact_preload_offset(chars, ((n >>  6) & 63) * 8, 8))
            .store_slice(__tact_preload_offset(chars, ((n      ) & 63) * 8, 8));
    }

    return res.end_cell().begin_parse();
}

(slice) __tact_address_to_user_friendly(slice address) inline_ref {
    (int wc, int hash) = address.parse_std_addr();

    slice user_friendly_address = begin_cell()
        .store_slice("11"s)
        .store_uint((wc + 0x100) % 0x100, 8)
        .store_uint(hash, 256)
    .end_cell().begin_parse();

    slice checksum = __tact_crc16(user_friendly_address);
    slice user_friendly_address_with_checksum = begin_cell()
        .store_slice(user_friendly_address)
        .store_slice(checksum)
    .end_cell().begin_parse();

    return __tact_base64_encode(user_friendly_address_with_checksum);
}

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

int __tact_slice_eq(slice a, slice b) inline {
    return (a.slice_hash() ==  b.slice_hash());
}

forall X0, X1, X2, X3, X4, X5, X6, X7 -> tuple __tact_tuple_create_8((X0, X1, X2, X3, X4, X5, X6, X7) v) asm """
    8 TUPLE
""";

forall X0, X1, X2, X3, X4, X5, X6, X7 -> (X0, X1, X2, X3, X4, X5, X6, X7) __tact_tuple_destroy_8(tuple v) asm """
    8 UNTUPLE
""";

tuple __tact_string_builder_start(builder b) inline {
    return tpush(tpush(empty_tuple(), b), null());
}

tuple __tact_string_builder_start_string() inline {
    return __tact_string_builder_start(begin_cell());
}

cell __tact_string_builder_end(tuple builders) inline {
    (builder b, tuple tail) = uncons(builders);
    cell c = b.end_cell();
    while(~ null?(tail)) {
        (b, tail) = uncons(tail);
        c = b.store_ref(c).end_cell();
    }
    return c;
}

slice __tact_string_builder_end_slice(tuple builders) inline {
    return __tact_string_builder_end(builders).begin_parse();
}

((tuple), ()) __tact_string_builder_append(tuple builders, slice sc) inline_ref {
    int sliceRefs = slice_refs(sc);
    int sliceBits = slice_bits(sc);

    while((sliceBits > 0) | (sliceRefs > 0)) {

        ;; Load the current builder
        (builder b, tuple tail) = uncons(builders);
        int remBytes = 127 - (builder_bits(b) / 8);
        int exBytes = sliceBits / 8;

        ;; Append bits
        int amount = min(remBytes, exBytes);
        if (amount > 0) {
            slice read = sc~load_bits(amount * 8);
            b = b.store_slice(read);
        }

        ;; Update builders
        builders = cons(b, tail);

        ;; Check if we need to add a new cell and continue
        if (exBytes - amount > 0) {
            var bb = begin_cell();
            builders = cons(bb, builders);
            sliceBits = (exBytes - amount) * 8;
        } elseif (sliceRefs > 0) {
            sc = sc~load_ref().begin_parse();
            sliceRefs = slice_refs(sc);
            sliceBits = slice_bits(sc);
        } else {
            sliceBits = 0;
            sliceRefs = 0;
        }
    }

    return ((builders), ());
}

int __tact_dict_get_slice_int(cell d, int kl, slice k, int vl) inline {
    var (r, ok) = __tact_dict_get(d, kl, k);
    if (ok) {
        return r~load_int(vl);
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_slice_int(cell d, int kl, slice k, int v, int vl) inline {
    if (null?(v)) {
        var (r, ok) = __tact_dict_delete(d, kl, k);
        return (r, ());
    } else {
        return (dict_set_builder(d, kl, k, begin_cell().store_int(v, vl)), ());
    }
}

int __tact_dict_get_int_int(cell d, int kl, int k, int vl) inline {
    var (r, ok) = idict_get?(d, kl, k);
    if (ok) {
        return r~load_int(vl);
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_int_int(cell d, int kl, int k, int v, int vl) inline {
    if (null?(v)) {
        var (r, ok) = idict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (idict_set_builder(d, kl, k, begin_cell().store_int(v, vl)), ());
    }
}

cell __tact_dict_get_int_cell(cell d, int kl, int k) inline {
    var (r, ok) = idict_get_ref?(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_int_cell(cell d, int kl, int k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = idict_delete?(d, kl, k);
        return (r, ());
    } else {
        return (idict_set_ref(d, kl, k, v), ());
    }
}

builder $global_beginCell() impure asm """
    NEWC
""";

slice $global_emptySlice() impure asm """
    b{} PUSHSLICE
""";

int $global_contractHash(cell $code, cell $data) impure asm """
    s0 PUSH HASHCU // `data` hash
    s2 PUSH HASHCU // `code` hash
    SWAP2
    CDEPTH         // `data` depth
    SWAP
    CDEPTH         // `code` depth
    131380 INT     // (2 << 16) | (1 << 8) | 0x34

    // Group 2: Composition of the Builder
    NEWC
    24 STU  // store refs_descriptor | bits_descriptor | data
    16 STU  // store depth_descriptor for `code`
    16 STU  // store depth_descriptor for `data`
    256 STU // store `code` hash
    256 STU // store `data` hash

    // Group 3: SHA256 hash of the resulting Builder
    ONE HASHEXT_SHA256
""";

slice $Slice$_fun_asAddressUnsafe(slice $self) impure asm "NOP";

slice $Cell$_fun_beginParse(cell $self) impure asm """
    CTOS
""";

slice $Cell$_fun_asSlice(cell $self) impure inline {
    var ($self) = $self;
    return $Cell$_fun_beginParse($self);
}

cell $Builder$_fun_endCell(builder $self) impure asm """
    ENDC
""";

slice $global_newAddress(int $chain, int $hash) impure inline {
    return $Slice$_fun_asAddressUnsafe($Cell$_fun_asSlice($Builder$_fun_endCell(store_uint(store_int(store_uint($global_beginCell(), 4, 3), $chain, 8), $hash, 256))));
}

slice $global_contractAddressExt(int $chain, cell $code, cell $data) impure inline {
    int $hash = $global_contractHash($code, $data);
    return $global_newAddress($chain, $hash);
}

slice $global_contractAddress((cell, cell) $s) impure inline {
    var (($s'code, $s'data)) = $s;
    return $global_contractAddressExt(0, $s'code, $s'data);
}

slice $global_myAddress() impure asm """
    MYADDR
""";

int $global_myBalance() impure asm """
    BALANCE FIRST
""";

() $global_nativeReserve(int $amount, int $mode) impure asm """
    RAWRESERVE
""";

() $global_message((int, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    106 PUSHINT         // 1 + 4 + 4 + 64 + 32 + 1
    STZEROES
    // → Stack state
    // s0: Builder
    // s1: `body`
    // s2: `mode`
    STDICT
    ENDC
    SWAP
    SENDRAWMSG
""";

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

int $global_now() impure asm """
    NOW
""";

slice $Int$_fun_toString(int $self) impure asm """
    <{
        // x
        NEWC // x b
        OVER // x b x
        0 LESSINT // x b <0?
        <{
            // x b
            45 PUSHINT // x b 45
            SWAP // x 45 b
            8 STU // x b
            SWAP // b x
            NEGATE // b -x
            SWAP // -x b
        }>CONT IF
        // x b

        SWAP // b x

        <{
            // b x
            10 PUSHINT DIVMOD // b x/10 x%10
            48 ADDCONST // b x/10 (x%10+48)
            s2 s2 s0 XC2PU ISZERO // (x%10+48) b x/10 x/10==0?
        }>CONT UNTIL
        // ... b x

        DROP // ... b
        DEPTH DEC // ... b n
        <{ 8 STU }>CONT REPEAT // b
    }>CONT 1 1 CALLXARGS
    // b

    ENDC CTOS // s
""";

((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), ()) $ToonGovernance$_fun_notify((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, cell $body) impure inline {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = $self;
    slice $to = __tact_context_get_sender();
    int $bounce = false;
    if (false) {
        int $balance = $global_myBalance();
        int $balanceBeforeMessage = ($balance - $Context$_get_value(__tact_context_get()));
        if (($balanceBeforeMessage < 0)) {
            $global_nativeReserve(0, 0);
            $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 130, $body));
            return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), ());
        }
    }
    $global_message($MessageParameters$_constructor_bounce_to_value_mode_body($bounce, $to, 0, 66, $body));
    return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), ());
}

;; ToonGovernance_ToonGovernance.constants.fc
;; String ":"
slice __gen_slice_string_d4945957eae362fe7564b6491c1f1933ef0e8c8d8c854b880ee9333b04f49c43() asm """
    B{b5ee9c724101010100030000023a5a06dd1a} B>boc <s PUSHSLICE
""";

;; String "emissionCap"
slice __gen_slice_string_755fd6f698049d8dc5a81f2524645157c01376c2c30934857f5c2f5fde5cc8fb() asm """
    B{b5ee9c7241010101000d000016656d697373696f6e436170387888f0} B>boc <s PUSHSLICE
""";

;; String "minWalletAge"
slice __gen_slice_string_479f97fea43a5b3fc7da07abea4028118286de6f7b8d13357f7c805233d30718() asm """
    B{b5ee9c7241010101000e0000186d696e57616c6c65744167652e329ff8} B>boc <s PUSHSLICE
""";

;; String "mintAuthority"
slice __gen_slice_string_05ff446710ed0d4d5203084880517594198484dd80982404a990009d6d1f75c1() asm """
    B{b5ee9c7241010101000f00001a6d696e74417574686f7269747931675077} B>boc <s PUSHSLICE
""";

;; ToonGovernance_ToonGovernance.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode_body(slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, null(), null(), $value, $to, true);
}

;;
;; Type: MessageParameters
;; TLB: _ mode:int257 body:Maybe ^cell value:int257 to:address bounce:bool = MessageParameters
;;

((int, cell, int, slice, int)) $MessageParameters$_constructor_bounce_to_value_mode_body(int $bounce, slice $to, int $value, int $mode, cell $body) inline {
    return ($mode, $body, $value, $to, $bounce);
}

;;
;; Type: DeployOk
;; Header: 0xaff90f57
;; TLB: deploy_ok#aff90f57 queryId:uint64 = DeployOk
;;

builder $DeployOk$_store(builder build_0, (int) v) inline {
    var (v'queryId) = v;
    build_0 = store_uint(build_0, 2952335191, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    return build_0;
}

cell $DeployOk$_store_cell((int) v, builder b) inline {
    return $DeployOk$_store(b, v).end_cell();
}

((int)) $DeployOk$_constructor_queryId(int $queryId) inline {
    return ($queryId);
}

;;
;; Type: TokenTransfer
;; Header: 0x0f8a7ea5
;; TLB: token_transfer#0f8a7ea5 queryId:uint64 amount:coins destination:address response_destination:address customPayload:Maybe ^cell forward_ton_amount:coins forward_payload:remainder<slice> = TokenTransfer
;;

builder $TokenTransfer$_store(builder build_0, (int, int, slice, slice, cell, int, slice) v) inline {
    var (v'queryId, v'amount, v'destination, v'response_destination, v'customPayload, v'forward_ton_amount, v'forward_payload) = v;
    build_0 = store_uint(build_0, 260734629, 32);
    build_0 = build_0.store_uint(v'queryId, 64);
    build_0 = build_0.store_varuint16(v'amount);
    build_0 = build_0.store_slice(v'destination);
    build_0 = __tact_store_address_opt(build_0, v'response_destination);
    build_0 = build_0.store_maybe_ref(v'customPayload);
    build_0 = build_0.store_varuint16(v'forward_ton_amount);
    build_0 = build_0.store_slice(v'forward_payload);
    return build_0;
}

cell $TokenTransfer$_store_cell((int, int, slice, slice, cell, int, slice) v, builder b) inline {
    return $TokenTransfer$_store(b, v).end_cell();
}

((int, int, slice, slice, cell, int, slice)) $TokenTransfer$_constructor_queryId_amount_destination_response_destination_customPayload_forward_ton_amount_forward_payload(int $queryId, int $amount, slice $destination, slice $response_destination, cell $customPayload, int $forward_ton_amount, slice $forward_payload) inline {
    return ($queryId, $amount, $destination, $response_destination, $customPayload, $forward_ton_amount, $forward_payload);
}

;;
;; Type: UpdateMintAuthority
;; Header: 0x787cca54
;; TLB: update_mint_authority#787cca54 newAuthority:address = UpdateMintAuthority
;;

builder $UpdateMintAuthority$_store(builder build_0, (slice) v) inline {
    var (v'newAuthority) = v;
    build_0 = store_uint(build_0, 2021444180, 32);
    build_0 = build_0.store_slice(v'newAuthority);
    return build_0;
}

cell $UpdateMintAuthority$_store_cell((slice) v, builder b) inline {
    return $UpdateMintAuthority$_store(b, v).end_cell();
}

((slice)) $UpdateMintAuthority$_constructor_newAuthority(slice $newAuthority) inline {
    return ($newAuthority);
}

;;
;; Type: UpdateConfigParam
;; Header: 0x0e849a55
;; TLB: update_config_param#0e849a55 parameter:^string newValue:uint64 = UpdateConfigParam
;;

builder $UpdateConfigParam$_store(builder build_0, (slice, int) v) inline {
    var (v'parameter, v'newValue) = v;
    build_0 = store_uint(build_0, 243571285, 32);
    build_0 = build_0.store_builder_ref(begin_cell().store_slice(v'parameter));
    build_0 = build_0.store_uint(v'newValue, 64);
    return build_0;
}

cell $UpdateConfigParam$_store_cell((slice, int) v, builder b) inline {
    return $UpdateConfigParam$_store(b, v).end_cell();
}

((slice, int)) $UpdateConfigParam$_constructor_parameter_newValue(slice $parameter, int $newValue) inline {
    return ($parameter, $newValue);
}

;;
;; Type: GlobalProposal
;; TLB: _ parameter:^string newValue:uint64 description:^string proposer:address votesFor:coins votesAgainst:coins deadline:uint32 executed:bool = GlobalProposal
;;

builder $GlobalProposal$_store(builder build_0, (slice, int, slice, slice, int, int, int, int) v) inline {
    var (v'parameter, v'newValue, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed) = v;
    build_0 = build_0.store_builder_ref(begin_cell().store_slice(v'parameter));
    build_0 = build_0.store_uint(v'newValue, 64);
    build_0 = build_0.store_builder_ref(begin_cell().store_slice(v'description));
    build_0 = build_0.store_slice(v'proposer);
    build_0 = build_0.store_varuint16(v'votesFor);
    build_0 = build_0.store_varuint16(v'votesAgainst);
    build_0 = build_0.store_uint(v'deadline, 32);
    build_0 = build_0.store_int(v'executed, 1);
    return build_0;
}

cell $GlobalProposal$_store_cell((slice, int, slice, slice, int, int, int, int) v, builder b) inline {
    return $GlobalProposal$_store(b, v).end_cell();
}

(slice, ((slice, int, slice, slice, int, int, int, int))) $GlobalProposal$_load(slice sc_0) inline {
    var v'parameter = sc_0~load_ref().begin_parse();
    var v'newValue = sc_0~load_uint(64);
    var v'description = sc_0~load_ref().begin_parse();
    var v'proposer = sc_0~load_msg_addr();
    var v'votesFor = sc_0~load_varuint16();
    var v'votesAgainst = sc_0~load_varuint16();
    var v'deadline = sc_0~load_uint(32);
    var v'executed = sc_0~load_int(1);
    return (sc_0, (v'parameter, v'newValue, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed));
}

tuple $GlobalProposal$_as_optional((slice, int, slice, slice, int, int, int, int) v) inline {
    var (v'parameter, v'newValue, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed) = v;
    return __tact_tuple_create_8(v'parameter, v'newValue, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed);
}

tuple $GlobalProposal$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $GlobalProposal$_as_optional(sc~$GlobalProposal$_load());
}

((slice, int, slice, slice, int, int, int, int)) $GlobalProposal$_tensor_cast((slice, int, slice, slice, int, int, int, int) v) asm "NOP";

((slice, int, slice, slice, int, int, int, int)) $GlobalProposal$_not_null(tuple v) inline {
    throw_if(128, null?(v));
    var (slice vvv'parameter, int vvv'newValue, slice vvv'description, slice vvv'proposer, int vvv'votesFor, int vvv'votesAgainst, int vvv'deadline, int vvv'executed) = __tact_tuple_destroy_8(v);
    return (vvv'parameter, vvv'newValue, vvv'description, vvv'proposer, vvv'votesFor, vvv'votesAgainst, vvv'deadline, vvv'executed);
}

tuple $GlobalProposal$_to_tuple(((slice, int, slice, slice, int, int, int, int)) v) inline {
    var (v'parameter, v'newValue, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed) = v;
    return __tact_tuple_create_8(v'parameter, v'newValue, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed);
}

tuple $GlobalProposal$_to_opt_tuple(tuple v) inline {
    if (null?(v)) { return null(); } 
    return $GlobalProposal$_to_tuple($GlobalProposal$_not_null(v)); 
}

tuple $GlobalProposal$_to_opt_external(tuple v) inline {
    var loaded = $GlobalProposal$_to_opt_tuple(v);
    if (null?(loaded)) {
        return null();
    } else {
        return (loaded);
    }
}

((slice, int, slice, slice, int, int, int, int)) $GlobalProposal$_constructor_parameter_newValue_description_proposer_votesFor_votesAgainst_deadline_executed(slice $parameter, int $newValue, slice $description, slice $proposer, int $votesFor, int $votesAgainst, int $deadline, int $executed) inline {
    return ($parameter, $newValue, $description, $proposer, $votesFor, $votesAgainst, $deadline, $executed);
}

;;
;; Type: AddressProposal
;; TLB: _ parameter:^string newAddress:address description:^string proposer:address votesFor:coins votesAgainst:coins deadline:uint32 executed:bool = AddressProposal
;;

builder $AddressProposal$_store(builder build_0, (slice, slice, slice, slice, int, int, int, int) v) inline {
    var (v'parameter, v'newAddress, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed) = v;
    build_0 = build_0.store_builder_ref(begin_cell().store_slice(v'parameter));
    build_0 = build_0.store_slice(v'newAddress);
    build_0 = build_0.store_builder_ref(begin_cell().store_slice(v'description));
    build_0 = build_0.store_slice(v'proposer);
    build_0 = build_0.store_varuint16(v'votesFor);
    build_0 = build_0.store_varuint16(v'votesAgainst);
    build_0 = build_0.store_uint(v'deadline, 32);
    build_0 = build_0.store_int(v'executed, 1);
    return build_0;
}

cell $AddressProposal$_store_cell((slice, slice, slice, slice, int, int, int, int) v, builder b) inline {
    return $AddressProposal$_store(b, v).end_cell();
}

(slice, ((slice, slice, slice, slice, int, int, int, int))) $AddressProposal$_load(slice sc_0) inline {
    var v'parameter = sc_0~load_ref().begin_parse();
    var v'newAddress = sc_0~load_msg_addr();
    var v'description = sc_0~load_ref().begin_parse();
    var v'proposer = sc_0~load_msg_addr();
    var v'votesFor = sc_0~load_varuint16();
    var v'votesAgainst = sc_0~load_varuint16();
    var v'deadline = sc_0~load_uint(32);
    var v'executed = sc_0~load_int(1);
    return (sc_0, (v'parameter, v'newAddress, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed));
}

tuple $AddressProposal$_as_optional((slice, slice, slice, slice, int, int, int, int) v) inline {
    var (v'parameter, v'newAddress, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed) = v;
    return __tact_tuple_create_8(v'parameter, v'newAddress, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed);
}

tuple $AddressProposal$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $AddressProposal$_as_optional(sc~$AddressProposal$_load());
}

((slice, slice, slice, slice, int, int, int, int)) $AddressProposal$_tensor_cast((slice, slice, slice, slice, int, int, int, int) v) asm "NOP";

((slice, slice, slice, slice, int, int, int, int)) $AddressProposal$_not_null(tuple v) inline {
    throw_if(128, null?(v));
    var (slice vvv'parameter, slice vvv'newAddress, slice vvv'description, slice vvv'proposer, int vvv'votesFor, int vvv'votesAgainst, int vvv'deadline, int vvv'executed) = __tact_tuple_destroy_8(v);
    return (vvv'parameter, vvv'newAddress, vvv'description, vvv'proposer, vvv'votesFor, vvv'votesAgainst, vvv'deadline, vvv'executed);
}

tuple $AddressProposal$_to_tuple(((slice, slice, slice, slice, int, int, int, int)) v) inline {
    var (v'parameter, v'newAddress, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed) = v;
    return __tact_tuple_create_8(v'parameter, v'newAddress, v'description, v'proposer, v'votesFor, v'votesAgainst, v'deadline, v'executed);
}

tuple $AddressProposal$_to_opt_tuple(tuple v) inline {
    if (null?(v)) { return null(); } 
    return $AddressProposal$_to_tuple($AddressProposal$_not_null(v)); 
}

tuple $AddressProposal$_to_opt_external(tuple v) inline {
    var loaded = $AddressProposal$_to_opt_tuple(v);
    if (null?(loaded)) {
        return null();
    } else {
        return (loaded);
    }
}

((slice, slice, slice, slice, int, int, int, int)) $AddressProposal$_constructor_parameter_newAddress_description_proposer_votesFor_votesAgainst_deadline_executed(slice $parameter, slice $newAddress, slice $description, slice $proposer, int $votesFor, int $votesAgainst, int $deadline, int $executed) inline {
    return ($parameter, $newAddress, $description, $proposer, $votesFor, $votesAgainst, $deadline, $executed);
}

;;
;; Type: ToonJettonWallet
;; TLB: _ balance:coins owner:address master:address = ToonJettonWallet
;;

builder $ToonJettonWallet$init$_store(builder build_0, (int, slice, slice) v) inline {
    var (v'balance, v'owner, v'master) = v;
    build_0 = build_0.store_varuint16(v'balance);
    build_0 = build_0.store_slice(v'owner);
    build_0 = build_0.store_slice(v'master);
    return build_0;
}

cell $ToonJettonWallet$_child_get_code() impure asm """
    B{} B>boc PUSHREF
""";

(cell, cell) $ToonJettonWallet$_init_child(int $balance, slice $owner, slice $master) inline_ref {
    ;; Build init code cell

    ;; Contract Code: ToonJettonWallet
    cell init_code = $ToonJettonWallet$_child_get_code();


    builder b = begin_cell();
    b = $ToonJettonWallet$init$_store(b, ($balance, $owner, $master));
    return (init_code, b.end_cell());
}

;;
;; Type: ToonGovernance
;; TLB: _ registry:address vault:address jettonMaster:address stakes:dict<address, int> totalStaked:coins proposals:dict<int, ^GlobalProposal{parameter:^string,newValue:uint64,description:^string,proposer:address,votesFor:coins,votesAgainst:coins,deadline:uint32,executed:bool}> nextProposalId:uint256 addressProposals:dict<int, ^AddressProposal{parameter:^string,newAddress:address,description:^string,proposer:address,votesFor:coins,votesAgainst:coins,deadline:uint32,executed:bool}> nextAddressProposalId:uint256 hasVoted:dict<int, bool> hasVotedAddress:dict<int, bool> = ToonGovernance
;;

builder $ToonGovernance$_store(builder build_0, (slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) v) inline {
    var (v'registry, v'vault, v'jettonMaster, v'stakes, v'totalStaked, v'proposals, v'nextProposalId, v'addressProposals, v'nextAddressProposalId, v'hasVoted, v'hasVotedAddress) = v;
    build_0 = build_0.store_slice(v'registry);
    build_0 = build_0.store_slice(v'vault);
    build_0 = build_0.store_slice(v'jettonMaster);
    build_0 = build_0.store_dict(v'stakes);
    build_0 = build_0.store_varuint16(v'totalStaked);
    var build_1 = begin_cell();
    build_1 = build_1.store_dict(v'proposals);
    build_1 = build_1.store_uint(v'nextProposalId, 256);
    build_1 = build_1.store_dict(v'addressProposals);
    build_1 = build_1.store_uint(v'nextAddressProposalId, 256);
    build_1 = build_1.store_dict(v'hasVoted);
    build_1 = build_1.store_dict(v'hasVotedAddress);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

(slice, ((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell))) $ToonGovernance$_load(slice sc_0) inline {
    var v'registry = sc_0~load_msg_addr();
    var v'vault = sc_0~load_msg_addr();
    var v'jettonMaster = sc_0~load_msg_addr();
    var v'stakes = sc_0~load_dict();
    var v'totalStaked = sc_0~load_varuint16();
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'proposals = sc_1~load_dict();
    var v'nextProposalId = sc_1~load_uint(256);
    var v'addressProposals = sc_1~load_dict();
    var v'nextAddressProposalId = sc_1~load_uint(256);
    var v'hasVoted = sc_1~load_dict();
    var v'hasVotedAddress = sc_1~load_dict();
    return (sc_0, (v'registry, v'vault, v'jettonMaster, v'stakes, v'totalStaked, v'proposals, v'nextProposalId, v'addressProposals, v'nextAddressProposalId, v'hasVoted, v'hasVotedAddress));
}

(slice, ((slice, slice, slice))) $ToonGovernance$init$_load(slice sc_0) inline {
    var v'registry = sc_0~load_msg_addr();
    var v'vault = sc_0~load_msg_addr();
    var v'jettonMaster = sc_0~load_msg_addr();
    return (sc_0, (v'registry, v'vault, v'jettonMaster));
}

(slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $ToonGovernance$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$ToonGovernance$_load();
    }
    else {
        (slice $registry, slice $vault, slice $jettonMaster) = $sc~$ToonGovernance$init$_load();
        $sc.end_parse();
        return $ToonGovernance$_contract_init($registry, $vault, $jettonMaster);
    }
}

() $ToonGovernance$_contract_store((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $ToonGovernance$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract ToonGovernance functions
;;

(slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $ToonGovernance$_contract_init(slice $registry, slice $vault, slice $jettonMaster) impure inline {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = (null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null());
    $self'registry = $registry;
    $self'vault = $vault;
    $self'jettonMaster = $jettonMaster;
    $self'totalStaked = 0;
    $self'nextProposalId = 0;
    $self'nextAddressProposalId = 0;
    return ($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress);
}

((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_voteKey((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId, slice $voter) impure inline_ref {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = $self;
    tuple $sb = __tact_string_builder_start_string();
    $sb~__tact_string_builder_append($Int$_fun_toString($proposalId));
    $sb~__tact_string_builder_append(__gen_slice_string_d4945957eae362fe7564b6491c1f1933ef0e8c8d8c854b880ee9333b04f49c43());
    $sb~__tact_string_builder_append(__tact_address_to_user_friendly($voter));
    var $fresh$ret_186 = __tact_sha256(__tact_string_builder_end_slice($sb));
    return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), $fresh$ret_186);
}

((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_quorumPassed((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, (slice, int, slice, slice, int, int, int, int) $proposal) impure inline_ref {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = $self;
    var (($proposal'parameter, $proposal'newValue, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed)) = $proposal;
    var $fresh$ret_187 = ( (($proposal'votesFor > $proposal'votesAgainst)) ? (($proposal'votesFor > ($self'totalStaked / 4))) : (false) );
    return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), $fresh$ret_187);
}

((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_quorumPassedAddr((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, (slice, slice, slice, slice, int, int, int, int) $proposal) impure inline_ref {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = $self;
    var (($proposal'parameter, $proposal'newAddress, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed)) = $proposal;
    var $fresh$ret_188 = ( (($proposal'votesFor > $proposal'votesAgainst)) ? (($proposal'votesFor > ($self'totalStaked / 4))) : (false) );
    return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), $fresh$ret_188);
}

((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_totalStaked((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self) impure inline_ref {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = $self;
    var $fresh$ret_189 = $self'totalStaked;
    return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), $fresh$ret_189);
}

((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_stake((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, slice $voter) impure inline_ref {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = $self;
    int $s = __tact_dict_get_slice_int($self'stakes, 267, $voter, 257);
    var $fresh$ret_190 = ((~ null?($s)) ? __tact_not_null($s) : 0);
    return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), $fresh$ret_190);
}

((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), tuple) $ToonGovernance$_fun_getProposal((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId) impure inline_ref {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = $self;
    var $fresh$ret_191 = $GlobalProposal$_load_opt(__tact_dict_get_int_cell($self'proposals, 257, $proposalId));
    return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), $fresh$ret_191);
}

((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), tuple) $ToonGovernance$_fun_getAddressProposal((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId) impure inline_ref {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = $self;
    var $fresh$ret_192 = $AddressProposal$_load_opt(__tact_dict_get_int_cell($self'addressProposals, 257, $proposalId));
    return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), $fresh$ret_192);
}

((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_hasAddressVoted((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId, slice $voter) impure inline_ref {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = $self;
    int $key = ($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)~$ToonGovernance$_fun_voteKey($proposalId, $voter);
    var $fresh$ret_193 = (~ null?(__tact_dict_get_int_int($self'hasVoted, 257, $key, 1)));
    return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), $fresh$ret_193);
}

((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_hasAddressVotedOnAddressProposal((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId, slice $voter) impure inline_ref {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = $self;
    int $key = ($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)~$ToonGovernance$_fun_voteKey($proposalId, $voter);
    var $fresh$ret_194 = (~ null?(__tact_dict_get_int_int($self'hasVotedAddress, 257, $key, 1)));
    return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), $fresh$ret_194);
}

((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell), int) $ToonGovernance$_fun_quorumMet((slice, slice, slice, cell, int, cell, int, cell, int, cell, cell) $self, int $proposalId) impure inline_ref {
    var (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)) = $self;
    tuple $p = $GlobalProposal$_load_opt(__tact_dict_get_int_cell($self'proposals, 257, $proposalId));
    if (null?($p)) {
        var $fresh$ret_195 = false;
        return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), $fresh$ret_195);
    }
    var $fresh$ret_196 = ($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)~$ToonGovernance$_fun_quorumPassed($GlobalProposal$_tensor_cast($GlobalProposal$_not_null($p)));
    return (($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress), $fresh$ret_196);
}

;;
;; Get methods of a Contract ToonGovernance
;;

_ %totalStaked() method_id(89512) {
    var self = $ToonGovernance$_contract_load();
    var res = self~$ToonGovernance$_fun_totalStaked();
    return res;
}

_ %stake(slice $voter) method_id(80984) {
    slice $voter = $voter;
    var self = $ToonGovernance$_contract_load();
    var res = self~$ToonGovernance$_fun_stake($voter);
    return res;
}

_ %getProposal(int $proposalId) method_id(84079) {
    int $proposalId = $proposalId;
    var self = $ToonGovernance$_contract_load();
    var res = self~$ToonGovernance$_fun_getProposal($proposalId);
    return $GlobalProposal$_to_opt_external(res);
}

_ %getAddressProposal(int $proposalId) method_id(65836) {
    int $proposalId = $proposalId;
    var self = $ToonGovernance$_contract_load();
    var res = self~$ToonGovernance$_fun_getAddressProposal($proposalId);
    return $AddressProposal$_to_opt_external(res);
}

_ %hasAddressVoted(int $proposalId, slice $voter) method_id(98948) {
    int $proposalId = $proposalId;
    slice $voter = $voter;
    var self = $ToonGovernance$_contract_load();
    var res = self~$ToonGovernance$_fun_hasAddressVoted($proposalId, $voter);
    return res;
}

_ %hasAddressVotedOnAddressProposal(int $proposalId, slice $voter) method_id(85734) {
    int $proposalId = $proposalId;
    slice $voter = $voter;
    var self = $ToonGovernance$_contract_load();
    var res = self~$ToonGovernance$_fun_hasAddressVotedOnAddressProposal($proposalId, $voter);
    return res;
}

_ %quorumMet(int $proposalId) method_id(99536) {
    int $proposalId = $proposalId;
    var self = $ToonGovernance$_contract_load();
    var res = self~$ToonGovernance$_fun_quorumMet($proposalId);
    return res;
}

;;
;; Routing of a Contract ToonGovernance
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress) = $ToonGovernance$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    var op = in_msg~load_opcode_internal();
    ;; Receive TokenNotification message
    if (op == 0x7362d09c) {
        var $msg'queryId = in_msg~load_uint(64);
        var $msg'amount = in_msg~load_varuint16();
        var $msg'from = in_msg~load_msg_addr();
        var $msg'forward_payload = in_msg;
        slice $expectedWallet = $global_contractAddress($ToonJettonWallet$_init_child(0, $global_myAddress(), $self'jettonMaster));
        throw_unless(2223, ( equal_slices_bits(__tact_context_get_sender(), $expectedWallet) ));
        int $existing = __tact_dict_get_slice_int($self'stakes, 267, $msg'from, 257);
        int $current = ((~ null?($existing)) ? __tact_not_null($existing) : 0);
        $self'stakes~__tact_dict_set_slice_int(267, $msg'from, ($current + $msg'amount), 257);
        $self'totalStaked = ($self'totalStaked + $msg'amount);
        $ToonGovernance$_contract_store(($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress));
        return ();
    }
    
    ;; Receive MockStake message
    if (op == 2952585114) {
        var $msg = empty_tuple();
        int $amount = 1000000000000;
        int $existing = __tact_dict_get_slice_int($self'stakes, 267, __tact_context_get_sender(), 257);
        int $current = ((~ null?($existing)) ? __tact_not_null($existing) : 0);
        $self'stakes~__tact_dict_set_slice_int(267, __tact_context_get_sender(), ($current + $amount), 257);
        $self'totalStaked = ($self'totalStaked + $amount);
        $ToonGovernance$_contract_store(($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress));
        return ();
    }

    ;; Receive UnstakeGovernance message
    if (op == 57173639) {
        var $msg'amount = in_msg~load_varuint16();
        int $existing = __tact_dict_get_slice_int($self'stakes, 267, __tact_context_get_sender(), 257);
        throw_unless(63274, ( ((~ null?($existing))) ? ((__tact_not_null($existing) >= $msg'amount)) : (false) ));
        int $newBal = (__tact_not_null($existing) - $msg'amount);
        $self'stakes~__tact_dict_set_slice_int(267, __tact_context_get_sender(), $newBal, 257);
        $self'totalStaked = ($self'totalStaked - $msg'amount);
        slice $jettonWallet = $global_contractAddress($ToonJettonWallet$_init_child(0, $global_myAddress(), $self'jettonMaster));
        $global_send($SendParameters$_constructor_to_value_mode_body($jettonWallet, 0, 64, $TokenTransfer$_store_cell($TokenTransfer$_constructor_queryId_amount_destination_response_destination_customPayload_forward_ton_amount_forward_payload(0, $msg'amount, __tact_context_get_sender(), __tact_context_get_sender(), null(), 0, $global_emptySlice()), begin_cell())));
        $ToonGovernance$_contract_store(($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress));
        return ();
    }
    
    ;; Receive ProposeParameterUpdate message
    if (op == 3148424161) {
        var $msg'parameter = in_msg~load_ref().begin_parse();
        var $msg'newValue = in_msg~load_uint(64);
        var $msg'description = in_msg~load_ref().begin_parse();
        int $weight = __tact_dict_get_slice_int($self'stakes, 267, __tact_context_get_sender(), 257);
        throw_unless(63731, ( ((~ null?($weight))) ? ((__tact_not_null($weight) > 0)) : (false) ));
        throw_unless(36734, ( (__tact_slice_eq($msg'parameter, __gen_slice_string_755fd6f698049d8dc5a81f2524645157c01376c2c30934857f5c2f5fde5cc8fb())) ? (true) : (__tact_slice_eq($msg'parameter, __gen_slice_string_479f97fea43a5b3fc7da07abea4028118286de6f7b8d13357f7c805233d30718())) ));
        int $pId = $self'nextProposalId;
        $self'nextProposalId = ($self'nextProposalId + 1);
        $self'proposals~__tact_dict_set_int_cell(257, $pId, $GlobalProposal$_store_cell($GlobalProposal$_constructor_parameter_newValue_description_proposer_votesFor_votesAgainst_deadline_executed($msg'parameter, $msg'newValue, $msg'description, __tact_context_get_sender(), 0, 0, ($global_now() + 1209600), false), begin_cell()));
        $ToonGovernance$_contract_store(($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress));
        return ();
    }
    
    ;; Receive VoteOnProposal message
    if (op == 3302738496) {
        var $msg'proposalId = in_msg~load_uint(256);
        var $msg'support = in_msg~load_int(1);
        tuple $p = $GlobalProposal$_load_opt(__tact_dict_get_int_cell($self'proposals, 257, $msg'proposalId));
        throw_unless(39639, (~ null?($p)));
        var ($proposal'parameter, $proposal'newValue, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed) = $GlobalProposal$_not_null($p);
        throw_unless(26849, ($global_now() <= $proposal'deadline));
        throw_unless(9622, (~ $proposal'executed));
        int $weight = __tact_dict_get_slice_int($self'stakes, 267, __tact_context_get_sender(), 257);
        throw_unless(61996, ( ((~ null?($weight))) ? ((__tact_not_null($weight) > 0)) : (false) ));
        int $key = ($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)~$ToonGovernance$_fun_voteKey($msg'proposalId, __tact_context_get_sender());
        throw_unless(22462, null?(__tact_dict_get_int_int($self'hasVoted, 257, $key, 1)));
        $self'hasVoted~__tact_dict_set_int_int(257, $key, true, 1);
        if ($msg'support) {
            $proposal'votesFor = ($proposal'votesFor + __tact_not_null($weight));
        } else {
            $proposal'votesAgainst = ($proposal'votesAgainst + __tact_not_null($weight));
        }
        $self'proposals~__tact_dict_set_int_cell(257, $msg'proposalId, $GlobalProposal$_store_cell(($proposal'parameter, $proposal'newValue, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed), begin_cell()));
        $ToonGovernance$_contract_store(($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress));
        return ();
    }
    
    ;; Receive ExecuteProposal message
    if (op == 3833516347) {
        var $msg'proposalId = in_msg~load_uint(256);
        tuple $p = $GlobalProposal$_load_opt(__tact_dict_get_int_cell($self'proposals, 257, $msg'proposalId));
        throw_unless(39639, (~ null?($p)));
        var ($proposal'parameter, $proposal'newValue, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed) = $GlobalProposal$_not_null($p);
        throw_unless(37276, ($global_now() > $proposal'deadline));
        throw_unless(9622, (~ $proposal'executed));
        throw_unless(2999, ($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)~$ToonGovernance$_fun_quorumPassed($GlobalProposal$_tensor_cast(($proposal'parameter, $proposal'newValue, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed))));
        $proposal'executed = true;
        $self'proposals~__tact_dict_set_int_cell(257, $msg'proposalId, $GlobalProposal$_store_cell(($proposal'parameter, $proposal'newValue, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed), begin_cell()));
        $global_send($SendParameters$_constructor_to_value_mode_body($self'registry, 0, 64, $UpdateConfigParam$_store_cell($UpdateConfigParam$_constructor_parameter_newValue($proposal'parameter, $proposal'newValue), begin_cell())));
        $ToonGovernance$_contract_store(($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress));
        return ();
    }
    
    ;; Receive ProposeAddressUpdate message
    if (op == 1506838652) {
        var $msg'parameter = in_msg~load_ref().begin_parse();
        var $msg'newAddress = in_msg~load_msg_addr();
        var $msg'description = in_msg~load_ref().begin_parse();
        int $weight = __tact_dict_get_slice_int($self'stakes, 267, __tact_context_get_sender(), 257);
        throw_unless(63731, ( ((~ null?($weight))) ? ((__tact_not_null($weight) > 0)) : (false) ));
        throw_unless(53903, __tact_slice_eq($msg'parameter, __gen_slice_string_05ff446710ed0d4d5203084880517594198484dd80982404a990009d6d1f75c1()));
        int $pId = $self'nextAddressProposalId;
        $self'nextAddressProposalId = ($self'nextAddressProposalId + 1);
        $self'addressProposals~__tact_dict_set_int_cell(257, $pId, $AddressProposal$_store_cell($AddressProposal$_constructor_parameter_newAddress_description_proposer_votesFor_votesAgainst_deadline_executed($msg'parameter, $msg'newAddress, $msg'description, __tact_context_get_sender(), 0, 0, ($global_now() + 1209600), false), begin_cell()));
        $ToonGovernance$_contract_store(($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress));
        return ();
    }
    
    ;; Receive VoteOnAddressProposal message
    if (op == 2081730446) {
        var $msg'proposalId = in_msg~load_uint(256);
        var $msg'support = in_msg~load_int(1);
        tuple $p = $AddressProposal$_load_opt(__tact_dict_get_int_cell($self'addressProposals, 257, $msg'proposalId));
        throw_unless(33397, (~ null?($p)));
        var ($proposal'parameter, $proposal'newAddress, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed) = $AddressProposal$_not_null($p);
        throw_unless(26849, ($global_now() <= $proposal'deadline));
        throw_unless(9622, (~ $proposal'executed));
        int $weight = __tact_dict_get_slice_int($self'stakes, 267, __tact_context_get_sender(), 257);
        throw_unless(61996, ( ((~ null?($weight))) ? ((__tact_not_null($weight) > 0)) : (false) ));
        int $key = ($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)~$ToonGovernance$_fun_voteKey($msg'proposalId, __tact_context_get_sender());
        throw_unless(22462, null?(__tact_dict_get_int_int($self'hasVotedAddress, 257, $key, 1)));
        $self'hasVotedAddress~__tact_dict_set_int_int(257, $key, true, 1);
        if ($msg'support) {
            $proposal'votesFor = ($proposal'votesFor + __tact_not_null($weight));
        } else {
            $proposal'votesAgainst = ($proposal'votesAgainst + __tact_not_null($weight));
        }
        $self'addressProposals~__tact_dict_set_int_cell(257, $msg'proposalId, $AddressProposal$_store_cell(($proposal'parameter, $proposal'newAddress, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed), begin_cell()));
        $ToonGovernance$_contract_store(($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress));
        return ();
    }
    
    ;; Receive ExecuteAddressProposal message
    if (op == 2175112412) {
        var $msg'proposalId = in_msg~load_uint(256);
        tuple $p = $AddressProposal$_load_opt(__tact_dict_get_int_cell($self'addressProposals, 257, $msg'proposalId));
        throw_unless(33397, (~ null?($p)));
        var ($proposal'parameter, $proposal'newAddress, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed) = $AddressProposal$_not_null($p);
        throw_unless(37276, ($global_now() > $proposal'deadline));
        throw_unless(9622, (~ $proposal'executed));
        throw_unless(2999, ($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)~$ToonGovernance$_fun_quorumPassedAddr($AddressProposal$_tensor_cast(($proposal'parameter, $proposal'newAddress, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed))));
        $proposal'executed = true;
        $self'addressProposals~__tact_dict_set_int_cell(257, $msg'proposalId, $AddressProposal$_store_cell(($proposal'parameter, $proposal'newAddress, $proposal'description, $proposal'proposer, $proposal'votesFor, $proposal'votesAgainst, $proposal'deadline, $proposal'executed), begin_cell()));
        if (__tact_slice_eq($proposal'parameter, __gen_slice_string_05ff446710ed0d4d5203084880517594198484dd80982404a990009d6d1f75c1())) {
            $global_send($SendParameters$_constructor_to_value_mode_body($self'registry, 0, 64, $UpdateMintAuthority$_store_cell($UpdateMintAuthority$_constructor_newAuthority($proposal'newAddress), begin_cell())));
        }
        $ToonGovernance$_contract_store(($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress));
        return ();
    }
    
    ;; Receive Deploy message
    if (op == 0x946a98b6) {
        var $deploy'queryId = in_msg~load_uint(64);
        ($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress)~$ToonGovernance$_fun_notify($DeployOk$_store_cell($DeployOk$_constructor_queryId($deploy'queryId), begin_cell()));
        $ToonGovernance$_contract_store(($self'registry, $self'vault, $self'jettonMaster, $self'stakes, $self'totalStaked, $self'proposals, $self'nextProposalId, $self'addressProposals, $self'nextAddressProposalId, $self'hasVoted, $self'hasVotedAddress));
        return ();
    }
    
    ;; Throw if not handled
    throw(130);
}


() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
        execute current@ context@ current!
        {
            // The core idea of this function is to save gas by avoiding unnecessary dict jump, when recv_internal/recv_external is called
            // We want to extract recv_internal/recv_external from the dict and select needed function
            // not by jumping to the needed function by it's index, but by using usual IF statements.

            }END> b> // Close previous builder, now we have a cell of previous code on top of the stack

            <{ // Start of the new code builder
                SETCP0
                // Swap the new code builder with the previous code, now we have previous code on top of the stack
                swap
                // Transform cell to slice and load first ref from the previous code, now we have the dict on top of the stack
                <s ref@

                // Extract the recv_internal from the dict
                dup 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot
                swap

                // Delete the recv_internal from the dict
                0 swap @procdictkeylen idict- drop
                // Delete the recv_external from the dict (it's okay if it's not there)
                -1 swap @procdictkeylen idict- drop
                // Delete the __tact_selector_hack from the dict
                65535 swap @procdictkeylen idict- drop

                // Bring the code builder from the bottom of the stack
                // because if recv_external extraction is optional, and the number of elements on the stack is not fixed
                depth 1- roll
                // Swap with the dict from which we extracted recv_internal and (maybe) recv_external
                swap

                // Check if the dict is empty
                dup null?
                // Store a copy of this flag in the bottom of the stack
                dup depth 1- -roll
                {
                    // If the dict is empty, just drop it (it will be null if it's empty)
                    drop
                }
                {
                    // If the dict is not empty, prepare continuation to be stored in c3
                    <{
                        // Save this dict as first ref in this continuation, it will be pushed in runtime by DICTPUSHCONST
                        swap @procdictkeylen DICTPUSHCONST
                        // Jump to the needed function by it's index
                        DICTIGETJMPZ
                        // If such key is not found, throw 11 along with the key as an argument
                        11 THROWARG
                    }> PUSHCONT
                    // Store the continuation in c3
                    c3 POP
                } cond

                // Function id is on top of the (runtime) stack
                DUP IFNOTJMP:<{
                    // place recv_internal here
                    DROP swap @addop
                }>

                // Bring back the flag, indicating if the dict is empty or not from the bottom of the stack
                depth 1- roll
                {
                    // If the dict is empty, throw 11
                    11 THROWARG
                }
                {
                    // If the dict is not empty, jump to continuation from c3
                    c3 PUSH JMPX
                } cond
            }> b>
        } : }END>c
        current@ context! current!
    } does @atend !
""";

() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}